/**
 * Authored data only: heroes, gear, acts, monsters, skills.
 *
 * Phase 1 moves the 4,639 lines of gameConfig.ts here, split by subject. It
 * stays free of rules — the engine reads this, never the other way round.
 */
export const CONTENT_VERSION = 0;
